<Window x:Class="MonitorRefreshRateSwitcher.HotkeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка горячей клавиши" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Нажмите комбинацию клавиш:" Margin="0,0,0,5"/>
        <TextBox x:Name="HotkeyTextBox" Grid.Row="1" 
                 IsReadOnly="True" 
                 Margin="0,0,0,10"
                 PreviewKeyDown="HotkeyTextBox_PreviewKeyDown"/>

        <TextBlock Text="Выберите частоту обновления:" Grid.Row="2" Margin="0,0,0,5"/>
        <ComboBox x:Name="RefreshRateComboBox" Grid.Row="3" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,5,0" Click="OkButton_Click"/>
            <Button Content="Отмена" Width="80" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window> 